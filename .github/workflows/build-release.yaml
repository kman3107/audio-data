---
name: Releases

on:  # yamllint disable-line rule:truthy
  push:
    tags:
      - 'v*'

# Cancel existing executions when new commits are pushed onto the branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pyinstaller-build:
    runs-on: ${{ matrix.os }}
    # Timeout of 15 mins
    timeout-minutes: 15
    strategy:
      matrix:
        # https://devguide.python.org/versions
        python-version: [ '3.11' ]
        os: [ windows-latest, ubuntu-latest, macos-latest ]
    steps:

      - name: Create Executable
        uses: sayyid5416/pyinstaller@v1
        with:
          python_ver: ${{ matrix.python-version }}
          spec: 'audio_data_gui.py'
          requirements: 'requirements.txt'
          upload_exe_with_name: edit_mp4
          options: >
            --onefile,
            --name "edit_mp4",
            --windowed,
            --icon favicon_light.ico,
            --add-data "favicon_light.ico:.",
            --collect-data customtkinter,
            --collect-data mutagen

  release-build:
    needs: pyinstaller-build
    runs-on: [ ubuntu-latest ]
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3

      - name: Download artifact
        uses: actions/download-artifact@v3
        with:
          name: edit_mp4
      - name: Display structure of downloaded files
        run: ls -R

      - name: Zip artifact for deployment
        run: |
          zip edit_mp4-linux.zip edit_mp4
          zip edit_mp4-windows.zip edit_mp4.exe
          zip edit_mp4-macos.zip edit_mp4.app/ -r

      - name: Upload artifact
        uses: ncipollo/release-action@v1
        with:
          prerelease: true
          generateReleaseNotes: true
          artifacts: "*.zip"
